<?xml version="1.0" encoding="UTF-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 選択時 -->
    <item android:state_checked="true">
        <shape android:shape="rectangle">
            <corners style="@style/radio" />
            <solid android:color="@color/bgdeepcolor" />
        </shape>
    </item>

    <!-- タップ時 -->
    <item android:state_pressed="true" >
        <shape android:shape="rectangle">
            <corners style="@style/radio" />
            <solid android:color="@color/radio_tap_color" />
            <stroke android:width="1dp" android:color="@color/bgdeepcolor" />
        </shape>
    </item>

    <!-- 未選択時 -->
    <item>
        <shape android:shape="rectangle">
            <corners style="@style/radio" />
            <solid android:color="@color/radio_white_color" />
            <stroke android:width="1dp" android:color="@color/bgdeepcolor" />
        </shape>
    </item>

</selector>