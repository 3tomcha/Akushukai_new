<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="site.kobatomo.akushukai">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--<activity android:name=".Activity.TestActivity"-->
            <!--android:label="@string/app_name"-->
            <!--&gt;-->
        <activity android:name=".Activity.MainActivity"
            android:label="@string/app_name"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.AddEventActivity" />
        <activity android:name=".Activity.ZenkokuAddPreActivity" />
        <activity android:name=".Activity.KobetsuEvent" />
        <activity android:name=".Activity.KobetsuAddEventActivity" />
        <activity android:name=".Activity.ZenkokuEvent" />
        <activity android:name=".Activity.ZenkokuAddEventActivity" />
        <activity android:name=".MenuEvent" />

    </application>

</manifest>